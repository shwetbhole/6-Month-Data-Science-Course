# Write a program that accepts a number and prints its absolute value.

num = int(input('Enter a number: '))

print('Absolute of value:', abs(num))

# If the number is positive or zero → it stays the same.
# If the number is negative → the minus sign is removed (becomes positive).